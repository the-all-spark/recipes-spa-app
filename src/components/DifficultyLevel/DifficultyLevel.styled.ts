import { styled } from '@mui/material';
import Box from '@mui/material/Box';

export const DifficultyLevelContainer = styled(Box)(() => ({
  width: '50px',
  display: 'flex',
  flexDirection: 'row',
  justifyContent: 'space-between',
  alignItems: 'center',
}));

export default DifficultyLevelContainer;
