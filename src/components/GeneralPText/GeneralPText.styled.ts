import { styled } from '@mui/material';
import Typography from '@mui/material/Typography';

const StyledGeneralPText = styled(Typography)(() => ({
  marginTop: '1rem',
  marginBottom: '1rem',
}));

export default StyledGeneralPText;
